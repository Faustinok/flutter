import 'package:apbar/ui/testes.dart';
import 'package:flutter/material.dart';

void main() => runApp(Apbar());

class Apbar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
      title: "AppBar",
      home: testeappbar(),
      
    );
  }
 
}